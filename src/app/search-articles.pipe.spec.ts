import { SearchArticlesPipe } from './search-articles.pipe';

describe('SearchArticlesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
